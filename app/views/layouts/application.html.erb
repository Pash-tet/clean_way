<!DOCTYPE html>
<html lang="ru" data-theme="cupcake" xmlns="http://www.w3.org/1999/html" class="h-full">
  <%= render partial: "shared/head" %>

  <body class="h-full">
    <div class="flex h-screen">
      <aside
        class="fixed top-0 left-0 h-screen w-64 -translate-x-full border-r border-r-gray-200 p-4 transition-transform md:translate-x-0"
      >
        <ul class="menu my-2">
          <% menu.each do |menu_item| %>
            <li>
              <%= link_to menu_item[:link] do %>
                <%= phosphor_icon menu_item[:icon], style: :duotone, width: 24 %>
                <%= menu_item[:label] %>
              <% end %>
            </li>
          <% end %>
        </ul>
      </aside>

      <div class="flex flex-1 flex-col md:ml-64">
        <nav class="fixed top-0 right-0 left-0 z-10 border-b border-b-gray-200 md:left-64">
          <%= render partial: "shared/navbar" %>
        </nav>

        <main class="mx-auto mt-16 flex-1 overflow-y-auto p-4"><%= yield %></main>
      </div>
    </div>
  </body>
</html>
