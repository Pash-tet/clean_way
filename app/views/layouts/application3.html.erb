<!DOCTYPE html>
<html lang="ru" data-theme="cupcake" xmlns="http://www.w3.org/1999/html" class="h-full">
  <%= render partial: "shared/head" %>

  <body>
    <div class="bg-base-100 drawer lg:drawer-open mx-auto max-w-[100rem]">
      <input id="drawer" type="checkbox" class="drawer-toggle" />
      <div class="drawer-content">
        <div
          class="bg-base-100/90 text-base-content sticky top-0 z-30 flex h-16 w-full [transform:translate3d(0,0,0)] justify-center backdrop-blur transition-shadow duration-100 print:hidden"
        >
          <%= render partial: "shared/navbar" %>
        </div>
        <div class="relative max-w-[100vw] px-6 pb-16 xl:pe-2"><%= yield %></div>
      </div>
      <div class="drawer-side z-40">
        <label for="drawer" class="drawer-overlay" aria-label="Close menu"></label>
        <aside class="bg-base-100 min-h-screen w-80">
          <div
            class="bg-base-100/90 navbar sticky top-0 z-20 hidden items-center gap-2 px-4 py-0 backdrop-blur lg:flex"
          >
            <div class="text-3xl">Clean Way</div>
          </div>
          <%= render partial: "shared/menu" %>
        </aside>
      </div>
    </div>
  </body>
</html>
